const utils = require("./utils");
const stringsUtils = require("./strings");

const result = stringsUtils.upperCase("hello");
console.log(result);

const sum = utils.addNumbers(2, 3);
console.log(sum);
